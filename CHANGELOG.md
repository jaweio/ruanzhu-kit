# 版本记录

## 1.0.6 — 2026-09-24

- 说明书 PDF 纸张由 Letter（216×279 mm）改为 A4（210×297 mm），与源程序 PDF 及登记材料要求一致；同一份说明书页数通常减少 1–3 页。

## 1.0.5 — 2026-09-24

- **朱雀检测改为必需闸门**：`材料上传清单`、看板和冒烟检查在未完成当前正文的朱雀检测时一律判为不可上传。新增 `zhusque_check.py decline`，只在用户本人明确拒绝时记录（需 `--user-declined` 和拒绝原话）；累计 2 次独立拒绝才记为“用户豁免”放行，清单与看板标注为“未检测·用户豁免”，不冒充已检测。桌面 App 增加“拒绝朱雀检测”按钮，每次需确认。
- 看板“可进入提交”的 AIGC 判定线改为 35 分，与 Step 6 `--fail-above 35` 闸门一致（原先 55 分以下即显示可提交）。
- 桌面 App 中 Agent 的 Bash 权限由黑名单改为白名单（`app/bash-policy.js`）：只放行 ruanzhu-kit 离线材料脚本（输出路径限定在材料目录，禁止 `--allow-upload/--allow-write/--allow-run/--jev`）和只读命令；拒绝重定向、命令替换、`python -c`、`sed -i`、`find -delete` 等写法。新增 `node --test` 回归用例。
- SKILL.md 8.2 表单示例去掉占位文字和与规则冲突的示例值（开发工具不填技术栈、硬件/系统不推测）。
- 修复冒烟测试与朱雀闸门不同步导致的失败；`软著材料/` 等本地产物不再纳入仓库。
- 包含此前未发布的纸面照片转 A4 PDF、AI 合规声明与 1.0.3/1.0.4 的全部改动（这两个版本此前未打标签发布）。

## 纸面照片转 A4 PDF（随 1.0.5 发布）

- 新增 `photo_to_a4_pdf.py`：将纸面照片按确认的四角校正为单页 A4 PDF；默认保留图像颜色并以无损 RGB 图像嵌入。
- 白底增强改为显式可选，不作为默认处理；补充拍摄照片转档说明和可选依赖预检。

## 1.0.4 — 2026-09-23

- 生成材料后由桌面 App 主动触发朱雀正式 `zhuque-text` 检测；未通过或接口失败不写入完成标记。
- 未配置 Key 时引导用户打开 EdgeOne Makers 获取页面，并生成网页检测回退说明；不再把聊天模型评分当作朱雀结果。
- 检测报告记录人工、AI、疑似 AI 比例；Key 仅在本机加密设置或 Keychain 中保存。
- 材料清单与看板校验朱雀完成标记对应当前正文/PDF 指纹；正文修改或正式 PDF 更新后，旧报告自动失效。
- 修复环境工具推断误读工具仓库自身 `.git` 的问题；补充官方朱雀接口路径、请求体和无 Key 网页交接回归测试。

## AI 合规声明（2026-09-22，随 1.0.5 发布）

- 新增 `ai_compliance.py` 与可复用 Word/正文模板；人工智能软件自动生成，其他软件支持按需开启，也可关闭。
- 常规 Word/PDF 生成、材料清单和看板同步支持声明；正文与文件名使用当前项目名称，桌面端/服务端分离。
- 权利人及签署日期缺失时留空；签章留白，使用适用范围内的承诺，不编造模型原创、备案或合规事实。
- 增加生成及配置继承、旧 PDF 失效、XML 转义、未签署状态、材料隔离等回归测试。
- 最终提交 PDF 统一输出到每份软著目录的 `提交材料/`；源码 DOCX/HTML、章节文件和 AI 合规声明编辑稿继续留在内部目录，申请表路径、材料清单、看板和旧目录兼容扫描同步更新。

## 1.0.3 — 2026-09-22

- 修复源码材料文件名使用“源码”时被版权检查器误当成普通软件文档的问题；检查器现在按正式材料角色、目录和新旧命名兼容识别程序材料。
- `材料上传清单.json` 增加正式材料范围（提交件、复核件、内部件）；版权检查和 AIGC 检测优先按清单扫描，排除旧版未裁剪、兼容和历史目录。
- 朱雀联网检测同样优先读取正式材料清单，只发送说明书、申请表文案和文档 PDF，不把源码 PDF 或历史材料带入请求。
- 新增 `preflight.py`：预检 Python 包、PDF 读取器、Pandoc、Chrome/Chromium、中文字体及可选 Node/Playwright 依赖。
- 补充材料清单、环境预检和工作流边界的回归测试；`requirements.txt` 增加 `pypdf`，用于 PDF 文本和页数回读。
- 说明书页数增加约 40 页的默认复核目标（35–45 页容差）；渲染和看板只提示偏离，不用空白页或重复内容凑页数。
- Jev 保持默认关闭，仅保留实验性代码，不接入填表主流程。
- **生成器不再输出任何占位符**：`application_form.py`、`generate_docs.py` 对缺失字段一律留空或省略，不再写 `【待填写/待改写/待补充/待生成…】`；缺失由校验清单列出（空必填字段判为“未填写”）。`form_plan.py`、`auto-fill.js` 同步把空必填字段视为未填写并拒绝填表。`待补充信息清单.md` 改为 `缺失信息清单.md`，只在确有缺失时生成；非后端项目不再生成空的 `截图证据计划.md`。
- 修复说明书第一段直接写入配置原值的问题：`summary`、`position` 未经过滤，默认模板会把“【待填写：软件类型】”写进“软件简介”；现统一经占位符过滤。`ruanzhu.config.example.json` 与 `auto-fill/config.template.json` 改为空值，删除虚构的硬件/系统默认值和“模块一功能说明”示例。
- **提交件零占位符硬闸门**：统一占位符规则（新增识别 `【截图预留：…】`、`【…占位…】`），`render_pdfs.py` 发现说明书含占位符时拒绝生成 PDF 并作废旧提交件；`artifact_manifest.py --verify` 抽取说明书 PDF 文本复核，含占位符或无法抽取文本均判为不可上传；看板中缺图不再提示“保留占位文字”。

## 1.0.2 — 2026-09-21

- 新增 `zhusque_check.py`：可选调用 EdgeOne Makers 兼容 OpenAI 的接口进行朱雀风格文风辅助检测。
- API Key 只从环境变量或本机 macOS Keychain 读取；未绑定时提示用户自行生成，检测前必须显式确认上传。
- 检测改为显式触发，并增加规范化文本块指纹和本机结果缓存，避免重复全文请求；支持 `--no-cache` 强制重测。
- `generate_docs.py` 首次生成后默认自动运行离线 AIGC 机械清理；新增 `zhusque_check.py finalize`，正文确认后执行一次全量朱雀检测并记录最终文本指纹。
- 新增 `prepare_demo_data.py`：从项目源码和说明书素材生成合成演示数据方案，跳过敏感字段和“测试/示例”占位词，供本地页面/API 创建数据后截图。
- 说明书改为“默认十章、按项目扩展”：支持 `projects[].extra_chapters`；材料分析、生成、检查和 PDF 流程明确不要求先启动前后端，运行项目仅用于截图、页面核验和演示数据创建。
- `render_pdfs.py` 增加 `reference` / `clean` 两种说明书排版样式，默认使用参考样式。
- 新增 `project_runtime.py`：仅在显式 `--allow-run` 时启动本地前后端，并提供状态、健康检查和停止。
- 新增 `apply_demo_data.py`：本地 API 演示数据默认预览，显式允许后写入，自动保存逆序回滚记录。
- 新增 `softcopyright_branch.py`：创建独立软著分支/工作树并检查多份材料源码重叠。
- `screenshots.py` 新增清单完整性检查，发现缺图、空图、未匹配项和缺失模块时可失败退出。
- `generate_source_docx.py` 默认对输出材料裁剪普通注释、导入/include/use 声明和连续空行；支持 `source_material` 配置及 `--keep-comments`、`--keep-imports` 覆盖，原项目源文件不变。
- 新增 `source_preview.py`：在生成 DOCX 前以本地 HTML 预览裁剪统计、跳过文件和脱敏样本；`generate_source_docx.py --preview` 可同步生成。
- 说明书默认内容不再填入硬件、权限、日志、安装命令、测试结果等未经核验的事实；缺失项改为待核验标记，并支持从项目配置提供真实值。
- `manual_spec.py` 从源码提取后端接口方法/路径/文件/行号，识别实际 Knife4j/OpenAPI 依赖并生成截图证据计划。
- `screenshots.py` 为后端截图写入 `evidence_id`，`--check --fail-on-missing` 可校验成功响应、4xx 错误响应、运行日志以及实际启用的调试页。
- AIGC 报告增加“可以/系统/模块”高频统计，识别通用优势/核心价值标题和“整套业务闭环是完整的”等生成式表达。

## 1.0.6 — 2026-09-24

- PDF 参考样式的章节页仅保留 `01-章节名` 单一标题，移除重复的“软件全称 - 章节名”副标题；新增修复同步规则，要求生成器变更同步到脚本、测试和文档。
- 源程序 PDF 改为由共享裁剪/脱敏后的 HTML 直接经 Chromium 打印生成；Python `python-docx` 继续提供可编辑 DOCX，移除 LibreOffice 作为必需的 DOCX→PDF 依赖。
- 新增可选 `jev_check.py` 与看板 `--jev --allow-upload`：使用 TypeSafe Jev 对有限材料摘要做 UI/交互、工程达标和人工复核闸门；默认关闭、Key 仅读环境变量、摘要按指纹缓存，不上传源程序。

## 1.0.1 — 2026-09-20

- 新增每次 Skill 新任务开始前检查最新正式 Release 的步骤。
- 新增 check_update.py：Git / 压缩包安装均支持；默认 4 秒网络超时，检查失败不阻塞任务。
- 发现新版只提示，不自动改写本地文件；无新版时继续工作。
- 增加版本比较、超时、限流、异常响应及不改写文件等 10 项测试。

## 1.0.0 — 2026-09-20

- 首次公开发布，基于当日本地 ruanzhu-manager 的 51 个文件。
- 添加 MIT 许可证、安装/更新说明、Git 安装管理工具及可复现的 `.skill` 打包工具。
- 添加版本号、依赖说明、更新保护测试和发布校验文件。
- PDF 工具检测移除个人目录硬编码，支持环境变量和 PATH。
- 修复备用填表脚本单独传入 `--check` 时被误当作配置文件名的问题。
- 保留原功能和排版；README 明确记录日期/发表状态旧规则、来源检查与文风评分等限制。
